> ** Agent Instructions:** 

Always use the `webapp` terminal environment activate the webapp env before running any terminal commands and PowerShell commands (Windows) when executing commands for this project.